// Shared client-side constants.

/**
 * Maximum length of a fan question. Mirrors the server's validation limit so
 * the input caps the field before a request can be rejected.
 */
export const MAX_QUESTION_LENGTH = 500;
